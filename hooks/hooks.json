{
  "description": "Myco boot and session-end rituals. See docs/architecture/L1_CONTRACT/protocol.md for R1 and R2. Uses the myco console script (installed by pyproject [project.scripts]) rather than python -m myco, so paths do not depend on whether the host's PATH resolves `python` or `python3` to the venv that installed myco.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python -m myco --json --project-dir \"$CLAUDE_PROJECT_DIR\" hunger"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python -m myco --json --project-dir \"$CLAUDE_PROJECT_DIR\" session-end"
          }
        ]
      }
    ]
  }
}
