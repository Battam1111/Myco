{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Battam1111/Myco/blob/main/docs/schema/canon.schema.json",
  "title": "Myco _canon.yaml",
  "description": "Machine-checkable shape of a Myco substrate's `_canon.yaml`. Governing doctrine: docs/architecture/L1_CONTRACT/canon_schema.md. This JSON-Schema is a second mechanical check alongside the Python kernel's `myco.core.canon.load_canon` validator — IDEs that understand JSON-Schema (VS Code, JetBrains, Neovim) can surface violations while the user edits canon, before `myco immune` runs.",
  "type": "object",
  "required": ["schema_version", "contract_version", "identity", "system", "subsystems"],
  "additionalProperties": true,
  "properties": {
    "schema_version": {
      "type": "string",
      "description": "Top-level shape version. v0.5.x canons declare \"1\"; unknown values trigger an upgrader lookup (`myco.core.canon.schema_upgraders`).",
      "pattern": "^[0-9]+$"
    },
    "contract_version": {
      "type": "string",
      "description": "The kernel contract version this canon was authored against. Must match `myco.__version__`'s contract form (e.g. \"v0.5.9\" or \"v0.5.9-alpha.1\").",
      "pattern": "^v?[0-9]+\\.[0-9]+\\.[0-9]+([-.][A-Za-z0-9]+(\\.[A-Za-z0-9]+)*)?$"
    },
    "synced_contract_version": {
      "type": "string",
      "description": "The contract version the substrate has last been assimilated against. Equals `contract_version` when no drift. Hunger reports drift when they differ; assimilate + CS1 resolve it.",
      "pattern": "^v?[0-9]+\\.[0-9]+\\.[0-9]+([-.][A-Za-z0-9]+(\\.[A-Za-z0-9]+)*)?$"
    },
    "identity": {
      "type": "object",
      "description": "Substrate self-identification. Consumed by every verb via `ctx.substrate.canon`.",
      "required": ["substrate_id", "entry_point"],
      "additionalProperties": true,
      "properties": {
        "substrate_id": {
          "type": "string",
          "description": "Globally-unique slug for this substrate. Matches `[A-Za-z][A-Za-z0-9_-]{0,63}`.",
          "pattern": "^[A-Za-z][A-Za-z0-9_-]{0,63}$",
          "examples": ["myco-self", "ascc-research"]
        },
        "tags": {
          "type": "array",
          "description": "Free-form affiliation tags. NOT project boundaries — L0 principle 2 forbids scope-gating.",
          "items": {"type": "string"},
          "default": []
        },
        "entry_point": {
          "type": "string",
          "description": "Filename of the agent entry page at substrate root (default \"MYCO.md\"; symbiont substrates may use \"CLAUDE.md\").",
          "default": "MYCO.md"
        }
      }
    },
    "system": {
      "type": "object",
      "description": "System-level invariants. Every verb that writes paths checks these.",
      "additionalProperties": true,
      "properties": {
        "write_surface": {
          "type": "object",
          "description": "L1 R6 enforcement — `allowed` is a list of fnmatch-style globs.",
          "required": ["allowed"],
          "properties": {
            "allowed": {
              "type": "array",
              "description": "Paths the substrate permits writes to. Any write outside raises WriteSurfaceViolation (exit 3) unless MYCO_ALLOW_UNSAFE_WRITE=1 is set.",
              "items": {"type": "string"},
              "minItems": 1
            }
          }
        },
        "hard_contract": {
          "type": "object",
          "description": "Pointer to the Hard Contract rules + their count.",
          "properties": {
            "rules_ref": {
              "type": "string",
              "description": "Relative path to the rules file (typically docs/architecture/L1_CONTRACT/protocol.md).",
              "default": "docs/architecture/L1_CONTRACT/protocol.md"
            },
            "rule_count": {
              "type": "integer",
              "description": "Number of rules in the Hard Contract. Must match R1-R7.",
              "const": 7
            }
          }
        },
        "no_llm_in_substrate": {
          "type": "boolean",
          "description": "L0 principle 1 addendum — `true` (default) means the substrate kernel does not import any LLM provider SDK. MP1/MP2 dims enforce mechanically. Opting out (`false`) requires a contract-bumping molt.",
          "default": true
        },
        "governance": {
          "type": "object",
          "description": "Risk-tier governance + Living Bets audit state. Originated at v0.6.0 (`public_window_*` + `last_winnowed_proposals` + token rotation); extended at v0.6.14 / v0.6.15 (cycle 自起 fruit—winnow—molt 闭环 fields); extended at v0.8.0 (schema v4) with the two Living Bets cache fields (`last_living_bets_audit_at` + `persistence_metrics`). All fields are additive; downstream consumers tolerate absence via `.get(...)` defaults. Authorising craft for v0.8.0 additions: `docs/primordia/v0_8_0_living_bets_amendment_2026-05-10.md` (and the v0.8.0 omnibus craft authorising the schema bump as item E).",
          "additionalProperties": true,
          "properties": {
            "last_living_bets_audit_at": {
              "type": ["string", "null"],
              "description": "Canon-cited marker for the most-recent Living Bets re-audit doc landing under `docs/primordia/`. ISO 8601 UTC timestamp (e.g. `2026-05-10T14:52:12Z`). Added at v0.8.0 (schema v4). `null` on canons that pre-date schema v4 or were observed cold by the upgrader chain; populated by `myco molt` at each contract bump (v0.8.x and later anamorph extension; out of scope for the v0.8.0 partial itself). LB1 dim reads canon-side marker before falling back to filesystem walk under `docs/primordia/**/*living_bets_audit*.md`. Authorising craft: `docs/primordia/v0_8_0_living_bets_amendment_2026-05-10.md`.",
              "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?(Z|[+-][0-9]{2}:[0-9]{2})$"
            },
            "persistence_metrics": {
              "type": "object",
              "description": "Cached persistence-budget signals consumed by the LB2 regime classifier (`src/myco/homeostasis/dimensions/semantic/lb2_living_bets_regime.py`). Three nullable integer counts — see `last_living_bets_audit_at` for the migration history. LB2 may either trust the cache (fast path) or recompute when the cache age exceeds a TTL (slow path); cache-policy lives in LB2, not in the schema. Added at v0.8.0 (schema v4). Authorising craft: `docs/primordia/v0_8_0_living_bets_amendment_2026-05-10.md`.",
              "additionalProperties": true,
              "properties": {
                "session_count": {
                  "type": ["integer", "null"],
                  "description": "Total distinct `session_id` values ever recorded in `.myco_state/` JSONL files. Proxy: 'how many distinct sessions has this substrate served?'.",
                  "minimum": 0
                },
                "host_count": {
                  "type": ["integer", "null"],
                  "description": "Number of host adapters that have evidence of write/install activity for this substrate. v0.8.0 MVP heuristic: `1` (this substrate at least has its own host running it).",
                  "minimum": 0
                },
                "peer_count": {
                  "type": ["integer", "null"],
                  "description": "Number of `identity.federation_peers` declared on this substrate. Mirrors the federation-peer cardinality at the time of last LB2 observation.",
                  "minimum": 0
                }
              }
            }
          }
        }
      }
    },
    "versioning": {
      "type": "object",
      "description": "Package-version sync pointers (consumed by SH1).",
      "properties": {
        "package_version_ref": {
          "type": "string",
          "description": "Relative path to the file declaring `__version__` (usually `src/myco/__init__.py`).",
          "default": "src/myco/__init__.py"
        },
        "pyproject_dynamic": {
          "type": "boolean",
          "description": "Whether `pyproject.toml` declares `version` as dynamic (reading from package_version_ref).",
          "default": true
        }
      }
    },
    "lint": {
      "type": "object",
      "description": "Lint-surface declaration. `dimensions_ref` points at the sibling file (`_canon_lint.yaml`) holding the per-dim definitions; `exit_policy.default` seeds the default `--exit-on` spec.",
      "properties": {
        "categories": {
          "type": "array",
          "description": "The four fixed categories from L1 exit_codes.md. Must be this exact set.",
          "items": {"enum": ["mechanical", "shipped", "metabolic", "semantic"]},
          "uniqueItems": true,
          "minItems": 4,
          "maxItems": 4
        },
        "dimensions_ref": {
          "type": "string",
          "description": "Relative path to the sibling lint-dimension file (e.g. `_canon_lint.yaml`). Loaded transparently by `core.canon._merge_lint_dimensions_subfile`. Single SSoT for the dim roster from v0.6.10."
        },
        "exit_policy": {
          "type": "object",
          "properties": {
            "default": {
              "type": "string",
              "description": "Default `--exit-on` spec. Grammar: \"<spec> ::= <global> | <per-cat-list>\" (see L1 exit_codes.md).",
              "default": "mechanical:critical,shipped:critical,metabolic:never,semantic:never"
            }
          }
        },
        "skeleton_downgrade": {
          "type": "object",
          "description": "Fresh-substrate severity downgrade. Keyed on the autoseeded marker file.",
          "properties": {
            "marker": {
              "type": "string",
              "description": "Path (relative) whose presence marks the substrate as skeleton mode.",
              "default": ".myco_state/autoseeded.txt"
            },
            "affected_dimensions": {
              "type": "array",
              "items": {"type": "string"},
              "description": "Dimension ids whose severity downgrades in skeleton mode. Empty at v0.5.x."
            }
          }
        }
      }
    },
    "subsystems": {
      "type": "object",
      "description": "L2 subsystem declarations. Each key is a canonical subsystem name; optional `doc` points at the L2 doctrine page, optional `package` points at the code tree. Both are additive — fresh substrates often declare subsystems without either (kernel's `load_canon` does not require them; only the subsystem NAMES are checked against MF1).",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "doc": {"type": "string", "description": "Optional relative path to the L2 doctrine page."},
          "package": {"type": "string", "description": "Optional relative path to the code package."}
        }
      }
    },
    "commands": {
      "type": "object",
      "properties": {
        "manifest_ref": {
          "type": "string",
          "description": "Relative path to the verb manifest YAML (`src/myco/surface/manifest.yaml`).",
          "default": "src/myco/surface/manifest.yaml"
        }
      }
    },
    "metrics": {
      "type": "object",
      "description": "Substrate-cited numbers. SE2 cross-checks these against observed reality.",
      "additionalProperties": true,
      "properties": {
        "test_count": {
          "type": "integer",
          "description": "Substrate-cited pytest count.",
          "minimum": 0
        },
        "lint_dim_count": {
          "type": ["integer", "null"],
          "description": "Substrate-cited count of registered immune lint dimensions. Added at v0.7.5 (schema v3). `null` on canons that pre-date schema v3 or were observed cold by the upgrader chain; populated to the live registry count on fresh `myco germinate` and refreshed by `scripts/bump_version.py` on every contract bump (closes the README/canon drift loop). Authorising craft: `docs/primordia/v0_7_5_p0_to_p6_omnibus_craft_2026-05-10.md` (P2).",
          "minimum": 0
        }
      }
    },
    "waves": {
      "type": "object",
      "description": "Contract-bump wave counter (incremented by `myco molt`).",
      "properties": {
        "current": {
          "type": "integer",
          "description": "Monotonic wave counter. Always increases on molt.",
          "minimum": 1
        },
        "log_ref": {
          "type": "string",
          "description": "Relative path to the contract changelog.",
          "default": "docs/contract_changelog.md"
        }
      }
    }
  }
}
