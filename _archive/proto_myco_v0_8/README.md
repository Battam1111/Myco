# Proto-Myco archive (v0.4 - v0.8.7)

> **Read-only archaeology.** These files are the v0.4 → v0.8.7 doctrine
> artifacts of proto-Myco (dead embryo per L0.5 Decision 3D).
> v0.9 substrate carries them ONLY as historical reference — none of
> their content is authoritative in v0.9.

## What is here

| File | What it was |
|---|---|
| `L0_VISION_proto.md` | The L0 vision authored 2026-04-15. Used by v0.4 - v0.8.7. Renamed "proto" because v0.8.x is dead embryo and the document describes the proto, not the mature Myco. |
| `L0_5_ESSENCE.md` | The transitional doctrine layer authored 2026-05-13 carrying 5 owner-arbitrated essence decisions. Its content has been **absorbed** into v0.9's new L0; this archived copy preserves the lineage. |
| `ESSENCE_BRAINSTORM.md` | The 2026-05-13 deliberation log. 4 self-corrections + 100%-confidence loop + 20 candidate holes + 10 fix-Hs. Records HOW the 5 decisions were reached. |

## Why this exists

L0.5 §7 (in this archive) declared its sunset trigger as "v0.9
ground-up rewrite kickoff". At kickoff (this branch's first commit),
L0.5 self-archives here; the new L0 carries forward its content.
This archive is the bookkeeping that makes the lineage auditable.

## How to read these

Do NOT treat them as authoritative. They describe the previous era's
attempt at Myco. v0.9 is a new attempt. The five 永恒X principles
(P1-P5) are stable across the boundary — but every β-layer choice in
the proto archive (verb names, dim count, subsystem partition,
schema shape, protocol surface, biological-verb mappings) is not
inherited by v0.9.

## How to find more lineage

- v0.8.x's full git history is reachable from the `main` branch
  HEAD `e47118f` (the last v0.8.x commit). See in particular the
  15 commits `6e8bfa0..e47118f` which together captured the
  v0.8.8 cleanup + post-archive calibration that closed the
  proto-Myco era.
- This `v0.9-genesis` branch is an orphan branch: its history does
  NOT extend back into `main`. The link between v0.8.x and v0.9 is
  purely doctrinal (this archive + L0.5 absorption) — not git-
  ancestry.
