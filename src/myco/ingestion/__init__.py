"""``myco.ingestion`` — 永恒吞噬 (eternal ingestion).

Governing doctrine: ``docs/architecture/L2_DOCTRINE/ingestion.md``.
Lands in Stage B.4.

Responsibility: take in anything the agent encounters, without an intake
filter. Raw captures land in ``notes/`` and progress through the
digestion pipeline separately. Commands: ``eat``, ``hunger``, ``sense``,
``forage``.
"""
