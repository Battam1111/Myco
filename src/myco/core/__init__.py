"""``myco.core`` — L1 primitives shared across every subsystem.

No intra-myco dependencies. Lands in Stage B.1.

Planned modules (per ``docs/architecture/L3_IMPLEMENTATION/package_map.md``):
``errors``, ``substrate`` (substrate-root discovery), ``paths``,
``canon`` (parse/validate ``_canon.yaml``), ``severity``, ``version``.
"""
