"""``myco.homeostasis.dimensions`` — per-dimension lint rules.

One module per dimension. Authored fresh in Stage B.8 against the
L1 + L2 needs of the rewrite, **not** ported from the v0.3 30-dimension
table (which lives in ``legacy_v0_3/`` for reference only).
"""
