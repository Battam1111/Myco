---
id: n_20260412T060604_94cf
status: extracted
source: eat
tags:
- wave30-seed
- compress-mvp
- forage-digest
created: '2026-04-12T06:06:04'
last_touched: '2026-04-12T09:51:59'
digest_count: 1
promote_candidate: false
excrete_reason: null
---
# Wave 30 evidence: myco compress MVP implementation bundle

Wave 30 implements myco compress per Wave 27 design (D1-D9) as kernel_contract MVP.

Bundle delivered (one subsystem per Wave 27 D8):

1. src/myco/io_utils.py (NEW, ~160 lines): atomic_write_text + atomic_write_yaml. First entry from hermes catalog C2 (atomic writes). Uses tempfile.mkstemp + os.replace in same directory for filesystem-level atomicity. Module docstring flags name as provisional pending Wave 29b biomimetic sweep (D6).

2. src/myco/compress_cmd.py (NEW, ~430 lines): full myco compress verb implementation. Functions: _project_root (mirrors notes_cmd strict mode), _resolve_cohort_by_tag (default eligible status {raw,digesting} + skip notes with compressed_from set as belt-and-suspenders), _resolve_cohort_by_ids (manual escape hatch, preserves input order per Wave 27 Attack H#2), _validate_cohort (cascade rejection + excreted rejection), _build_output_body (markdown scaffold), _build_output_meta (output frontmatter with all 4 audit fields + aggregated input tags), _build_input_update (excreted + compressed_into + pre_compression_status), _execute_compression (two-phase commit: phase 1 build all in memory, phase 2 atomic_write_text output first then iterate inputs, best-effort warn on per-input failure with explicit recovery instructions), run_compress (CLI dispatch). Exit codes: 0 ok, 2 usage, 3 validation, 4 empty cohort, 5 io.

3. src/myco/cli.py: compress subparser added with note_ids (nargs=*), --tag, --rationale (required prose), --status, --confidence (float default 0.85), --dry-run, --json, --project-dir. Dispatch case routes to compress_cmd.run_compress.

4. _canon.yaml: contract_version v0.25.0 -> v0.26.0; synced_contract_version v0.25.0 -> v0.26.0; optional_fields extended with compressed_from / compressed_into / compression_method / compression_rationale / compression_confidence / pre_compression_status; valid_sources adds compress; new notes_schema.compression section with ripe_threshold=5 and ripe_age_days=7. src/myco/templates/_canon.yaml mirrored.

5. src/myco/notes.py: VALID_SOURCES extended with forage (was missing from prior wave!) AND compress; OPTIONAL_FIELDS extended with 6 compression audit fields; new detect_compression_ripe function (~100 lines) scans raw notes by tag and computes cohort size + oldest age, returns advisory non-blocking compression_ripe signal when both thresholds met; compute_hunger_report wires the new detector into signals list per Wave 27 D2.

6. src/myco/metabolism.py: re-export updated to include detect_compression_ripe.

7. src/myco/lint.py: new lint_compression_integrity (~130 lines, 3 checks): output integrity (status=extracted, source=compress, method+rationale set, compressed_from members exist), input back-link (compressed_into points back, status=excreted), cascade prevention (no compressed_from input may have its own compressed_from), orphan detection (compressed_into pointing to nonexistent note). Registered as L18 in main() check list. Module docstring updated 18-Dimension -> 19-Dimension.

8. src/myco/immune.py: re-export updated to include lint_compression_integrity.

9. tests/unit/test_compress.py (NEW, ~280 lines): 5 tests + 2 helpers. Tests: test_compress_consumptive_with_audit (Wave 27 D3+D4 audit shape, 3-note cohort, verifies output extracted+source=compress+all 4 audit fields, verifies each input excreted+compressed_into+pre_compression_status=raw, verifies L18 accepts), test_compress_dry_run_no_writes (Wave 27 Attack G defense, captures mtime+content before, runs --dry-run, verifies no mutation, no new files), test_compress_cascade_rejected (Wave 27 §2.1 defense #4, compresses 3 to output1, attempts to compress output1 by id, verifies exit 3 validation error, verifies no mutation), test_compress_idempotent_empty_cohort (Wave 27 §1.7, compresses 5 to 1 = 6 files, second run same tag exits 4 with no new files), test_lint_compression_integrity_catches_orphan (L18 backstop, deletes output file, verifies L18 fires HIGH with broken audit chain message).

R2.3 mid-round bug catch: First pytest run caught a silent idempotence bug in _resolve_cohort_by_tag — the output extracted note inherited the aggregated input tags via _build_output_meta, so on the second compress run with the same tag it got re-resolved, hit cascade rejection at validation time, and exited 3 instead of empty-cohort exit 4. Wave 27 §1.7 idempotence guarantee was silently violated by an incomplete resolver filter. Fixed: default eligible-status filter is {raw, digesting}, AND resolver explicitly skips notes that already have compressed_from set as belt-and-suspenders. This is the strongest possible empirical validation of the Wave 25 tests infrastructure investment — the test suite caught a real silent-failure bug in the implementation craft on its first run.

Verification: lint 19/19 PASS (L18 + 18 prior dimensions), pytest tests/ -q 9/9 PASS (4 prior + 5 new), boot brief regenerated (L16 cleared after canon edit), structural_bloat advisory at 43 (over by 3) acknowledged for deferred archive sweep, session_end_drift gear2 advisory at 17 entries acknowledged.

Anchor #4 (压缩即认知) implementation_coverage estimated 0.35 -> ~0.75 post-Wave-30. Anchor #3 (seven-step pipeline) estimated 0.43 -> ~0.75 post-Wave-30 (compress folds steps 2+3+5+7 into one verb per Wave 27 D6).

Honest edges:
- Two-phase commit is best-effort, not transactional. Torn-state window exists between output write and last input write. Mitigated by L18 lint backstop + loud stderr warning + explicit recovery instructions citing the output id.
- compression_method is free-form. Documented as L2 limitation. Future wave can elevate to enum trivially.
- io_utils.py name is provisional pending Wave 29b biomimetic sweep. Module docstring flags this.
- myco uncompress verb is still vapor. Wave 27 D5 deferred to Wave 29+. Wave 30 preserves the data (pre_compression_status field is written) but no uncompress verb exists. Manual recovery is mechanical because data is on disk and audit chain is L18-enforced.
- Hallucination risk is bounded, not eliminated. Wave 27 L1 honest framing inherited.
- compression_ripe is single-tag per hunger run. Acceptable for MVP.
- Multi-tag input notes have order-dependent aggregation (deterministic by note id sort but not semantic).

Craft of record: docs/primordia/compress_mvp_craft_2026-04-12.md (kernel_contract, 3 rounds, 0.91 confidence — target 0.90 met +0.01 from R2.3 empirical validation).
