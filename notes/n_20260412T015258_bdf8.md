---
id: n_20260412T015258_bdf8
status: integrated
source: eat
tags:
- wave25
- landing
- hermes-absorption
- engineering-craftsmanship
- integrated-candidate
created: '2026-04-12T01:52:58'
last_touched: '2026-04-12T01:53:03'
digest_count: 1
promote_candidate: false
excrete_reason: null
---
# Wave 25 landed — v0.24.0 — tests infrastructure seed (hermes absorption C1)

**Date**: 2026-04-12
**Craft**: `docs/primordia/hermes_absorption_craft_2026-04-12.md` (kernel_contract, 3 rounds, 0.90)
**Evidence**: `notes/n_20260412T013044_5546.md` (deep forage-digest research bundle)
**Parent raw** (this note's ancestor): n_20260412T013044_5546

## What landed

First concrete absorption from the hermes-agent deep-read craft. Converts the meta-hole "9132 lines of production Python, zero test files" from an invisible structural gap into a standing regression surface.

1. `tests/` top-level with `conftest.py::_isolate_myco_project` autouse fixture (redirects every test into `tmp_path/project/` with minimal `_canon.yaml` + `notes/` + `docs/primordia/` + empty `log.md`; clears stray env vars; inserts `src/` into sys.path; `monkeypatch.chdir` into the fixture project).

2. `tests/unit/test_notes.py` with 4 seed tests covering the notes module's most load-bearing paths:
   - frontmatter contract round-trip (`write_note` → `read_note`)
   - `serialize_note` ↔ `parse_frontmatter` inverse property (unicode + symbols)
   - `write_note` id-collision retry branch (monkeypatched `generate_id` forces the while-loop deterministically)
   - `_project_root` raises `MycoProjectNotFound` on orphan directories — **Wave 20 silent-fail regression guard**

3. `pyproject.toml`: `[project.optional-dependencies].dev = {pytest>=7,<9, pytest-xdist>=3,<4}` and `[tool.pytest.ini_options]` with `testpaths = ["tests"]`, `integration` marker, `addopts = "-m 'not integration'"`. Scope lock prevents the Wave 23 forage-crawl scar class from ever recurring on a bare `pytest` invocation.

4. `_canon.yaml::system.tests` — new SSoT section: `test_dir: tests/`, `min_unit_tests: 4`, `integration_marker: integration`, `unit_subdir: tests/unit/`. Mirrored into `src/myco/templates/_canon.yaml` with `min_unit_tests: 0` so downstream instances start at zero.

5. `scripts/install_git_hooks.sh`: `MYCO_PRECOMMIT_PYTEST=1` gate now resolves `test_dir` from canon via inline Python (PyYAML already a runtime dep), falls back to hardcoded `tests` on any error. Single-source-of-truth discipline — hook + pytest no longer have two literal copies of the path.

6. Contract v0.23.0 → v0.24.0 in kernel canon + template canon + `docs/contract_changelog.md`.

## Decisions that become canon

These are the craft's §4.1 D1–D6 decisions, now load-bearing:

- **D1 — Catalog exists, is NOT roadmap**. The 20-item hermes-pattern catalog (C1–C20) in the craft is permanent engineering-knowledge reference, not a schedule. Future waves cite it by item code.

- **D2 — Wave 25 = tests only**. Four unit tests are the seed, not coverage. Tests is the ONE T1 absorption grounded in 3 of 5 recent scars (W20, W23, W24) — all other T1 candidates (atomic writes, error taxonomy, logging) are defensible absorptions but match hermes's historical order, not Myco's friction order.

- **D3 — Wave 26+ is friction-driven, NOT schedule-driven**. The first bug the new test suite catches determines the subject of Wave 26. The catalog's T1 items are candidates, not commitments. Each future wave runs its own craft.

- **D4 — Three revisions from the original catalog stand**:
  - `myco doctor` → extend `myco hunger --doctor` (no new surface)
  - Three-phase compression → hot-gate only (no LLM summarizer in notes loop)
  - Command registry → downgraded T1 → T2 (9 verbs × 2 surfaces, ROI insufficient for bleeding)

- **D5 — Substrate/runtime boundary is immutable**. Wave 25 did NOT authorize absorption of: agent loop, gateway platforms, cron, ~/.hermes home scoping, OAuth, credential pool, terminal backends, MCP clients, runtime compression. Any future craft attempting to reopen these must re-cite `MYCO.md §身份锚点`.

- **D6 — The Wave 9 surface-level digest `n_20260411T183722_c1b2` is NOT superseded, it is completed**. The earlier note was a Hard-Contract pressure test that explicitly deferred source reading. This craft + note cycle is the deferred read.

## Wave 25 landing discipline (record)

- Single subsystem (tests infra) per Wave 23-24 observed cadence (1-2 subsystems per wave).
- NOT packaged with atomic writes / error taxonomy / logging despite all four being T1 candidates, because Round 2 "capacity attack" established that the observed wave ceiling is 2 subsystems, not 4.
- `myco lint` green across all 18 dimensions before landing.
- `pytest tests/ -q` green (4 unit tests pass).
- Craft protocol (`docs/craft_protocol.md`) reflex satisfied: all trigger surfaces touched by this landing were pre-authorized by the craft.

## What Wave 25 did NOT do

- No CI wiring. GitHub Actions deferred to when CI friction appears.
- No coverage measurement. Coverage discipline is a future wave.
- No refactor of any existing src/myco/ file beyond what was required to make the autouse fixture work (and that required zero edits — the fixture only adds).
- No atomic writes, error taxonomy, structured logging, command registry, or doctor command. Those are Wave 26+ candidates, ordering determined by friction from Wave 25's test suite.

## Related
- [Wave 26 landed — Vision Re-Audit + doctrine reconciliation](n_20260412T024944_3e32.md) — next wave in landing sequence
- [Wave 27 landed — Forward Compression primitive design](n_20260412T030457_6717.md) — subsequent landing in same session
- [Compressed synthesis — engineering craftsmanship waves](n_20260412T232248_6396.md) — hermes absorption wave cluster synthesis
