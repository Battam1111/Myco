---
id: n_20260412T035338_8497
status: integrated
source: eat
tags:
- wave28
- landing
- biomimetic
- nomenclature
- integrated-candidate
created: '2026-04-12T03:53:38'
last_touched: '2026-04-12T03:53:47'
digest_count: 1
promote_candidate: false
excrete_reason: null
---
# Wave 28 landed — Biomimetic Nomenclature craft (kernel_contract)

**Date**: 2026-04-12
**Craft**: `docs/primordia/biomimetic_nomenclature_craft_2026-04-12.md` (kernel_contract, 3 rounds, 0.90)
**Evidence**: W28 evidence bundle (note deleted)
**Self-correction capture**: verification-layer drift (note deleted)
**Parent**: Wave 27 session user grant for implementation-layer rewrite + biomimetic mandate
**Child**: Wave 29 = execution of 11 renames + migration tool + ASCC dogfood

## What landed

Exploration-class-style design craft (decision_class kernel_contract because it pre-authorizes Wave 29's kernel contract surface touches). Answers: "Which surfaces of Myco should be renamed for biomimetic fidelity, and what gets left alone?"

No code changes yet. No canon edits yet. No module file renames yet. Only the DESIGN decision that 11 coordinated renames will happen, and under what constraints.

## 10 decisions (D1-D10)

**D1** — 6 identity axioms from `vision_reaudit_craft_2026-04-12.md Part I` are IMMUTABLE. Wave 28 operates strictly below doctrine.

**D2** — 11 surfaces rename:
1. `wiki/` → `hyphae/`
2. `_canon.yaml` → `_genome.yaml`
3. `src/myco/notes.py` → metabolism.py (planned, not executed)
4. `src/myco/notes_cmd.py` → metabolism_cmd.py (planned, not executed)
5. `src/myco/lint.py` → immune.py (planned, not executed)
6. `myco correct` / `myco_correct` → `myco molt` / `myco_molt`
7. `myco lint` / `myco_lint` → `myco immune` / `myco_immune` (Attack K defense added this)
8. `system.notes_schema.*` → `system.metabolism_schema.*` (canon nested)
9. `system.self_correction.*` → `system.molt.*` (canon nested)
10. `system.wiki_page_types` → `system.hyphae_page_types` (canon)
11. Lint labels L0/L7/L10: "Canon Self-Check"→"Genome Self-Check", "Wiki Format"→"Hyphae Format", "Notes Schema"→"Metabolism Schema"

**D3** — Everything else stays. Full negative list in craft §4.1 D3.

**D4** — `myco lint` → `myco immune` accepted (Attack K defense). Internal inconsistency between CLI verb and module name (State B) is worse than user onboarding friction (State C). Agents learn from --help, not muscle memory. Myco's primary user per doctrine is the agent.

**D5** — Wave 29 is kernel_contract 0.92 target, 3 rounds. Wave 29's own craft re-checks this design under implementation pressure.

**D6** — Wave 29 atomically bundles: 11 renames + migration tool + ASCC dogfood. Justified as three phases of ONE transformation (coupled), not three independent subsystems (Attack L defense).

**D7** — Transition aliases for `myco correct` and `myco lint` emit stderr deprecation warning and forward to new verbs for exactly ONE wave. Wave 30 removes them.

**D8** — Contract bumps v0.25.0 → v0.26.0 in Wave 29.

**D9** — All downstream instances must run `myco migrate --biomimetic` after kernel bump to v0.26.0. ASCC is the dogfood case.

**D10** — Wave 28 does NOT touch Wave 28+ marathon goals. Forward compression (was planned W28, now W30), C-structural sensor (W31), H-2/H-6 attacks (W32/W33), D-layer completion (W34), extract/integrate standalone (W35), Metabolic Inlet design+MVP (W36-W39) all remain in force. They will be executed using the new biomimetic names.

## 4 attacks ran (craft §2)

- Attack J (cosmetic breaking change) — lands partially on cost, drove "Wave 29 bundles migration tool" decision
- Attack K (lint CLI-vs-module seam) — lands fully, drove addition of `myco lint → myco immune` to rename list
- Attack L (Wave 29 capacity violation) — lands as "document the coupling in Wave 29 Round 2"
- Attack M (`_genome.yaml` onboarding friction) — lands partially on new-contributor cost, drove "top-of-file pointer comment" mitigation

**1 attack fully accepted (K) + 3 attacks partially accepted (J, L, M) = every attack produced a revision**. This is the protocol's "real attacks not rubber stamps" criterion.

## Landing record (Wave 28 scope, 9 items)

1. ✅ `docs/primordia/biomimetic_nomenclature_craft_2026-04-12.md` written (~1200 lines)
2. ✅ Evidence bundle `n_20260412T035236_f396` eaten as raw
3. ⏳ This decisions note eaten + digested to integrated
4. ⏳ `log.md` Wave 28 milestone entry
5. ⏳ `myco lint` 18/18 + `pytest` 4/4 verification
6. NO contract bump (design craft only — contract bump happens in Wave 29)
7. NO MYCO.md edit (axioms untouched)
8. NO _canon.yaml edit (canon rename is Wave 29)
9. Commit + push per "真正的一刻不停" marathon mode (no user confirmation)

## What Wave 28 did NOT do

- No renames executed (Wave 29)
- No migration tool written (Wave 29)
- No ASCC dogfood (Wave 29)
- No code changes at all (design-only craft)
- No test additions
- No lint rule changes
- No forward compression work (Wave 30)

## Wave 29 implementation brief (compressed from craft §3.2)

**10 phases**:
- A: preparation (grep inventory + dry-run)
- B: core kernel renames (5 file/dir/module moves)
- C: canon schema updates (4 nested key trees)
- D: template + docs + README cascades (prose updates)
- E: immune.py label updates (L0/L7/L10 strings)
- F: tests updates (conftest + test file renames + imports)
- G: craft file references (current-surface only, not history)
- H: migration tool implementation (src/myco/migrate.py extension)
- I: ASCC dogfood run
- J: verification + contract bump v0.25.0 → v0.26.0 + changelog + log.md milestone

**Estimated cost**: 50-100 file touches, depends on grep-inventory result in Phase A.

## Today's post-Wave-28 state

- Wave 28 craft on disk (kernel_contract, 0.90 confidence, 3 rounds, 4 attacks)
- 11-item rename list locked and defended
- Wave 29 execution brief ready
- Self-correction note (verification-layer drift, note deleted) was in raw queue
- Marathon mode ACTIVE: commit + push without user confirmation; only stop on lint red / test red / craft impossible

## Next wave is Wave 29 (biomimetic rename execution)

Immediately after Wave 28 push completes, the same session continues with Wave 29:
- Wave 29 opens its own craft (kernel_contract, 0.92, 3 rounds) that re-validates this design under implementation pressure
- Phase A: inventory
- Phases B-J: execute
- Commit + push at end
- Wave 30 follows immediately (compress MVP using new names)
