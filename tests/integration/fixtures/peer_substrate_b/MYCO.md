# peer-fixture-b

Minimal fixture peer substrate (B) used by `tests/integration/test_propagate_3peer_network.py` to exercise `myco propagate`'s N-peer fan-out against three real second substrates (a, b, c).
