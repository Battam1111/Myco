{
  "name": "@myco/anchor-client",
  "version": "0.9.0-alpha.1",
  "description": "Myco v0.9 — anchor-client (anchor/client): owner-side rendering + signing + nonce + heartbeat (L0 cards/AS_anchor_surface + L1/GOVERNANCE §2)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --no-warnings --test --test-reporter=spec tests/*.test.ts",
    "test:all": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0"
  }
}
